Vocabulary Definitions

function-Functions are one of the fundamental building blocks in JavaScript. 
A function is a JavaScript procedure—a set of statements that performs a task or calculates a value.
To use a function, you must define it somewhere in the scope from which you wish to call it.

onload-The onload event occurs when an object has been loaded. onload is most often used within the <body> element
 to execute a script once a web page has completely loaded all content (including images, script files, CSS files, etc.).

.innerHTML-The innerHTML property can be used to modify your document's HTML on the fly. When you use innerHTML , you can change the page's content without refreshing the page. ... The innerHTML property can be used along with getElementById() 
within your JavaScript code to refer to an HTML element and change its contents.

script-The <script> tag is used to define a client-side script (JavaScript). The <script> element either contains script statements, or it points to an external script file through the src attribute. ... 
To select an HTML element, JavaScript most often uses the document.getElementById() method.

html-"Hypertext Markup Language." HTML is the language used to create webpages. "

body-is used to contain a web page's content, including hyperlinks, images, tables, text, etc

event-JavaScript's interaction with HTML is handled through events that occur when the user or the browser manipulates a page.
 When a page is loading it is called an event.

handlers- A function or method containing program statements that are executed in response to an event. An event handler typically is a software routine that processes actions such as keystrokes and mouse movements. ...
 JavaScript is a common method of scripting event handlers for Web content.

onclick-

data-

Console-

boolean-

number-

+= -

/= -

== -

value-


getElemetById-

If-

else-

Constant-

Var- Variables